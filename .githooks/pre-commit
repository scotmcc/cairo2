#!/bin/sh
unformatted=$(gofmt -l cmd internal 2>/dev/null)
if [ -n "$unformatted" ]; then
    echo "gofmt: the following files need formatting:" >&2
    echo "$unformatted" | sed 's/^/  /' >&2
    echo "Run: gofmt -w cmd internal" >&2
    exit 1
fi

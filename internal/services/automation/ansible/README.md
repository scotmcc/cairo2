# internal/services/automation/ansible

**Status:** 🔲 SLOT

Ansible automation connector. Exposes playbook execution as agent tools.

Config key: `ansible_inventory`, `ansible_vault_password_file` in `store/config/`.

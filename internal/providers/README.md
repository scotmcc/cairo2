# internal/providers

**Layer:** Foundation  
**Status:** ✅ working, moves as-is

Environment context injection — git status, shell state, VS Code workspace, WaveTerm context. Feeds into the agent's system prompt assembly.

Source: `~/cairo/internal/providers/`.

# internal/store/jobs

Jobs, tasks, worktrees, task artifacts, reap (cleanup of stale jobs), and platform process management (proc_unix, proc_windows).

Source: `~/cairo/internal/db/` (job-related files).
